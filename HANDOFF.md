# Day 1 Handoff (From Member 5)

This document summarizes the handoff from Member 5 (Risk Engine) to the rest of the InfraGuard-AI team for Day 2 execution.

## To Members 3 & 4 (ML Engineers)
- I have established the framework for the Overall Risk Score (`Cost Risk + Schedule Risk + Progress Risk`).
- **Your Job on Day 2**: When you train your XGBoost/Random Forest models to predict `cost_overrun_flag` and `time_overrun_flag`, output the **probabilities** (e.g., 0.85 chance of cost overrun).
- On Day 2, I will replace my static, retrospective "Score 0-3" heuristics with your forward-looking ML probabilities to create a true *Predictive* Risk Score.

## To Member 6 (Dashboard / Full Stack)
- I have generated the prototype logic for Risk Categories (LOW, MEDIUM, HIGH, CRITICAL).
- Your dashboard MUST highlight the **106 CRITICAL projects** (see `outputs/member5_critical_projects.csv`).
- **UI Elements Needed**:
  - A summary pie/donut chart of the Risk Categories (refer to `outputs/member5_risk_distribution.png`).
  - A scatter plot showing the "Progress Gap" (Physical Progress vs Expenditure), colored by Risk Category (refer to `outputs/member5_progress_vs_expenditure.png`).
  - A bar chart of the highest-risk Agencies.
- **Rule Engine**: Expose the "Early Warning Rules" (e.g., 'High Expenditure with Low Progress') as clickable filters in the dashboard so MoSPI officials can instantly see which projects are violating which rules.

## What Member 5 Will Build on Day 2
- I will integrate the ML probabilities generated by Members 3 and 4 into the composite Risk Engine.
- I will generate the final `paimana_scored_v2.csv` dataset containing the exact risk scores, categories, and triggered Early Warning rules for all 1,775 projects.
- I will provide the JSON schema that Member 6 will use to feed this data into the frontend.
